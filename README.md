What is the difference between var, let, and const?
var is global scope and var can redclare
let is global scope but can't redeclar
const is constant value and not able to redclare but can change value inside const..

What is the spread operator (...)?
to use concat two object with one object without missing any data..

What is the difference between map(), filter(), and forEach()?
map for find all data from array, filter is search data from array and forEach is display individual object like array.

What is an arrow function?
arrow function is new version function of ES6 and there have no need to return It.

What are template literals?
We can show template literals using back tick `` so this is template literals.
